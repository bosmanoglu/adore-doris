if [ -e "${i_resfile}" ]; then
  ipython -pylab -i -c "import os; os.sys.path.append(os.path.join(os.path.dirname(os.environ['ADORESCR']), 'lib/python')); import adore; ires=adore.res2dict('${i_resfile}'); mres=adore.res2dict('${m_resfile}');sres=adore.res2dict('${s_resfile}');"
else
  ipython -pylab -i -c "import os; os.sys.path.append(os.path.join(os.path.dirname(os.environ['ADORESCR']), 'lib/python')); import adore; mres=adore.res2dict('${m_resfile}');sres=adore.res2dict('${s_resfile}');"
fi
